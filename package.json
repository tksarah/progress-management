{
  "name": "progress-tracker-poc",
  "private": true,
  "version": "0.6.2",
  "workspaces": [
    "frontend"
  ],
  "scripts": {
    "dev": "tauri dev",
    "dev:web": "npm --workspace frontend run dev",
    "build": "tauri build",
    "build:web": "npm --workspace frontend run build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.1.0"
  },
  "dependencies": {
    "exceljs": "^4.4.0"
  }
}
